# Testes

Testes em Node puro (sem framework) para funções utilitárias e validadores
do backend Apps Script. Para rodar:

```bash
node tests/run.js
```

Saída esperada: lista de testes com ✓/✗ e total.

CI: o workflow `.github/workflows/tests.yml` roda automaticamente em cada push.
